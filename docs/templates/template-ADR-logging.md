## ADR (шаблон): Подход к логированию

Используйте этот шаблон для фиксации решений по логированию:
- формат логов (текст / JSON);
- уровни логирования;
- структура сообщений;
- места логирования (API, доменная логика, инфраструктура);
- требования по хранению и анализу логов.
